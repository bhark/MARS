apiVersion: v1
kind: Service
metadata:
  name: mars-postgis
  labels:
    app.kubernetes.io/part-of: postgis
    app.kubernetes.io/component: postgis
spec:
  selector:
    app.kubernetes.io/component: postgis
  ports:
    - name: pg
      port: 5432
      targetPort: pg
      protocol: TCP
