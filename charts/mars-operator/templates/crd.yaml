# Auto-generated by scripts/generate-crd.sh. Do not edit by hand.
# Source of truth: bin/mars-operator/src/crd/{spec,cluster}.rs
# helm.sh/resource-policy: keep makes helm uninstall leave the CRDs
# intact so existing MarsService/MarsServiceCluster objects survive
# operator removal.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  name: marsserviceclusters.mars.forn.dk
spec:
  group: mars.forn.dk
  names:
    categories: []
    kind: MarsServiceCluster
    plural: marsserviceclusters
    shortNames:
    - msvcc
    singular: marsservicecluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MarsServiceClusterSpec via `CustomResource`
        properties:
          spec:
            description: MarsServiceCluster - one cluster-scoped CR per logical deployment cluster.
            properties:
              artifactStore:
                description: Artifact store + cache. Parsed at reconcile into `mars_config::Artifacts`.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              defaults:
                default: {}
                description: Cluster-wide compiler/render tuning defaults consumed at compose time.
                properties:
                  compiler:
                    nullable: true
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  render:
                    nullable: true
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              observability:
                description: |-
                  Cluster-wide observability defaults. Parsed into
                  `mars_config::Observability`.
                nullable: true
                type: object
                x-kubernetes-preserve-unknown-fields: true
              reprojection:
                description: |-
                  Cluster-default reprojection allowlist. Narrowed by
                  `MarsService.spec.reprojection` at compose time via intersection.
                  Parsed into `mars_config::Reprojection`.
                nullable: true
                type: object
                x-kubernetes-preserve-unknown-fields: true
              sourcesCatalog:
                default: []
                description: |-
                  Logical source catalog. Each entry carries a stable `id`; per-service
                  `MarsService.spec.sources` references those ids. Parsed at reconcile
                  into `Vec<mars_config::Source>`.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
            required:
            - artifactStore
            type: object
        required:
        - spec
        title: MarsServiceCluster
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  name: marsservices.mars.forn.dk
spec:
  group: mars.forn.dk
  names:
    categories: []
    kind: MarsService
    plural: marsservices
    shortNames:
    - msvc
    singular: marsservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MarsServiceSpec via `CustomResource`
        properties:
          spec:
            description: MarsService - one CR per logical MARS service in a namespace.
            properties:
              clusterRef:
                description: |-
                  Pointer to the cluster-scoped `MarsServiceCluster` catalog this service
                  composes against.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              compiler:
                properties:
                  affinity:
                    description: |-
                      Opaque k8s Affinity. Mirrors the upstream `corev1.Affinity` shape;
                      kube validates the structure server-side. Deserialised at build
                      time so a malformed CR fails reconcile rather than the apiserver
                      admission.
                    nullable: true
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  env:
                    items:
                      properties:
                        name:
                          type: string
                        value:
                          nullable: true
                          type: string
                        valueFrom:
                          nullable: true
                          properties:
                            configMapKeyRef:
                              nullable: true
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                                optional:
                                  nullable: true
                                  type: boolean
                              required:
                              - key
                              - name
                              type: object
                            fieldRef:
                              nullable: true
                              properties:
                                apiVersion:
                                  nullable: true
                                  type: string
                                fieldPath:
                                  type: string
                              required:
                              - fieldPath
                              type: object
                            secretKeyRef:
                              nullable: true
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                                optional:
                                  nullable: true
                                  type: boolean
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  envFrom:
                    items:
                      properties:
                        configMapRef:
                          nullable: true
                          properties:
                            name:
                              type: string
                            optional:
                              nullable: true
                              type: boolean
                          required:
                          - name
                          type: object
                        prefix:
                          nullable: true
                          type: string
                        secretRef:
                          nullable: true
                          properties:
                            name:
                              type: string
                            optional:
                              nullable: true
                              type: boolean
                          required:
                          - name
                          type: object
                      type: object
                    type: array
                  imagesConfigMap:
                    description: |-
                      Optional ConfigMap (in the same namespace) carrying bitmap files
                      referenced from styles via `FillPaint::Image { name }`. When set,
                      the operator mounts it read-only at `/var/lib/mars/images`; the
                      MARS config must point `compiler.images_dir` at the same path so
                      the compiler resolves the names during pack.
                    nullable: true
                    type: string
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  resources:
                    description: |-
                      Resource requests/limits. Mirrors k8s ResourceRequirements but kept
                      schema-friendly here so we never tie the CRD to a specific k8s-openapi
                      minor.
                    nullable: true
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  storage:
                    default:
                      cacheSize: 1Gi
                      storageClass: ''
                      workSize: 2Gi
                    properties:
                      cacheSize:
                        default: 1Gi
                        type: string
                      storageClass:
                        default: ''
                        type: string
                      workSize:
                        default: 2Gi
                        type: string
                    type: object
                  tolerations:
                    items:
                      description: |-
                        Schema-friendly mirror of k8s_openapi Toleration. Same rationale as
                        ResourceRequirementsSpec: avoid coupling the CRD schema to the
                        k8s-openapi `schemars` feature.
                      properties:
                        effect:
                          nullable: true
                          type: string
                        key:
                          nullable: true
                          type: string
                        operator:
                          nullable: true
                          type: string
                        tolerationSeconds:
                          format: int64
                          nullable: true
                          type: integer
                        value:
                          nullable: true
                          type: string
                      type: object
                    type: array
                type: object
              definition:
                description: |-
                  `RenderDefinition` source: sibling-key oneOf (`inline`, `configMapRef`,
                  `gitRef`, `s3Ref`). Exactly one variant must be set.
                properties:
                  configMapRef:
                    description: In-cluster `ConfigMap` carrying the `RenderDefinition` under `key`.
                    nullable: true
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  gitRef:
                    description: Git repository hosting the `RenderDefinition` file.
                    nullable: true
                    properties:
                      gitRef:
                        description: Exactly one of `branch`, `tag`, `commit`. Enforced at adapter-resolve time.
                        properties:
                          branch:
                            nullable: true
                            type: string
                          commit:
                            nullable: true
                            type: string
                          tag:
                            nullable: true
                            type: string
                        type: object
                      interval:
                        description: Poll cadence. Defaults to `1m` at the adapter when unset.
                        nullable: true
                        type: string
                      path:
                        type: string
                      secretRef:
                        description: Credential bundle. Omit for public repos.
                        nullable: true
                        properties:
                          name:
                            type: string
                        required:
                        - name
                        type: object
                      url:
                        type: string
                    required:
                    - gitRef
                    - path
                    - url
                    type: object
                  inline:
                    description: Literal `RenderDefinition` YAML embedded on the CR.
                    nullable: true
                    type: string
                  s3Ref:
                    description: S3-compatible object store hosting the `RenderDefinition` file.
                    nullable: true
                    properties:
                      bucket:
                        type: string
                      endpoint:
                        description: Override for non-AWS endpoints. AWS S3 when unset.
                        nullable: true
                        type: string
                      interval:
                        description: Poll cadence. Defaults to `1m` at the adapter when unset.
                        nullable: true
                        type: string
                      key:
                        type: string
                      region:
                        type: string
                      secretRef:
                        description: |-
                          Credential bundle. Falls back to the default `object_store` chain
                          (env, IRSA, instance profile) when omitted.
                        nullable: true
                        properties:
                          name:
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - bucket
                    - key
                    - region
                    type: object
                type: object
              reprojection:
                description: |-
                  Service-side reprojection narrowing. Intersected against the cluster
                  default at compose time. Parsed into `mars_config::Reprojection` at
                  reconcile; kept opaque in the CRD schema for the same reason as
                  `MarsServiceCluster.spec.reprojection`.
                nullable: true
                type: object
                x-kubernetes-preserve-unknown-fields: true
              runtime:
                properties:
                  affinity:
                    nullable: true
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  cache:
                    default:
                      sizeLimit: 1Gi
                      storageClass: ''
                    properties:
                      sizeLimit:
                        default: 1Gi
                        type: string
                      storageClass:
                        default: ''
                        type: string
                    type: object
                  env:
                    items:
                      properties:
                        name:
                          type: string
                        value:
                          nullable: true
                          type: string
                        valueFrom:
                          nullable: true
                          properties:
                            configMapKeyRef:
                              nullable: true
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                                optional:
                                  nullable: true
                                  type: boolean
                              required:
                              - key
                              - name
                              type: object
                            fieldRef:
                              nullable: true
                              properties:
                                apiVersion:
                                  nullable: true
                                  type: string
                                fieldPath:
                                  type: string
                              required:
                              - fieldPath
                              type: object
                            secretKeyRef:
                              nullable: true
                              properties:
                                key:
                                  type: string
                                name:
                                  type: string
                                optional:
                                  nullable: true
                                  type: boolean
                              required:
                              - key
                              - name
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  envFrom:
                    items:
                      properties:
                        configMapRef:
                          nullable: true
                          properties:
                            name:
                              type: string
                            optional:
                              nullable: true
                              type: boolean
                          required:
                          - name
                          type: object
                        prefix:
                          nullable: true
                          type: string
                        secretRef:
                          nullable: true
                          properties:
                            name:
                              type: string
                            optional:
                              nullable: true
                              type: boolean
                          required:
                          - name
                          type: object
                      type: object
                    type: array
                  extraVolumeMounts:
                    description: |-
                      Extra `corev1.VolumeMount` entries appended to the runtime container.
                      Each entry must reference a volume name defined in `extraVolumes` (or
                      a reserved internal one). Validated at reconcile time.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  extraVolumes:
                    description: |-
                      Extra `corev1.Volume` entries appended to the runtime pod spec after
                      the operator-managed volumes (config, cache, optional artifact-store).
                      Opaque in the CRD schema and validated at reconcile time; the names
                      must not collide with the reserved internals (`config`, `cache`,
                      `artifact-store`). Primary use: mounting Secrets/ConfigMaps/PVCs of
                      custom font files so `service.fonts.paths` can pick them up.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  podDisruptionBudget:
                    description: |-
                      Override for the runtime PodDisruptionBudget. When omitted, the
                      operator auto-creates a PDB with `maxUnavailable: 1` for any
                      multi-replica runtime (`replicas > 1`) and creates none for a single
                      replica. Setting this field replaces that default with the given
                      spec; clearing it restores the default.
                    nullable: true
                    properties:
                      maxUnavailable:
                        description: Integer (`"1"`) or percentage string (`"50%"`).
                        nullable: true
                        type: string
                      minAvailable:
                        description: Integer (`"2"`) or percentage string (`"50%"`).
                        nullable: true
                        type: string
                    type: object
                  replicas:
                    default: 2
                    format: int32
                    type: integer
                  resources:
                    description: |-
                      Schema-friendly mirror of k8s_openapi ResourceRequirements. Conversion to
                      the wire type happens at child-render time. We mirror the structure rather
                      than depend on the k8s-openapi `schemars` feature to keep the CRD stable
                      across k8s-openapi minor bumps.
                    nullable: true
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  service:
                    default:
                      metricsScrape: true
                      port: 8080
                      serviceType: ClusterIP
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Extra annotations to set on the runtime Service. Useful for ingress
                          controllers, scrape overrides, cloud-LB hints. Merged on top of any
                          operator-managed defaults (user keys win on collision).
                        type: object
                      metricsScrape:
                        default: true
                        description: |-
                          Emit the standard `prometheus.io/{scrape,port,path}` annotations on
                          the runtime Service so Prometheus pod/service-monitor scrape discovers
                          it out of the box. User-supplied `annotations` win on key collision,
                          so this stays compatible with non-Prometheus stacks (Datadog, etc.).
                        type: boolean
                      port:
                        default: 8080
                        format: int32
                        type: integer
                      serviceType:
                        default: ClusterIP
                        type: string
                    type: object
                  tolerations:
                    items:
                      description: |-
                        Schema-friendly mirror of k8s_openapi Toleration. Same rationale as
                        ResourceRequirementsSpec: avoid coupling the CRD schema to the
                        k8s-openapi `schemars` feature.
                      properties:
                        effect:
                          nullable: true
                          type: string
                        key:
                          nullable: true
                          type: string
                        operator:
                          nullable: true
                          type: string
                        tolerationSeconds:
                          format: int64
                          nullable: true
                          type: integer
                        value:
                          nullable: true
                          type: string
                      type: object
                    type: array
                type: object
              sources:
                description: |-
                  Logical source ids to pull from the cluster catalog. Each entry must
                  resolve to a `sourcesCatalog[].id` on the referenced cluster.
                items:
                  type: string
                type: array
            required:
            - clusterRef
            - compiler
            - definition
            - runtime
            - sources
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              definition:
                description: |-
                  Identity of the last successfully fetched RenderDefinition. Cleared
                  while `DefinitionResolved` is not True.
                nullable: true
                properties:
                  observed:
                    description: |-
                      Identity of the most recently fetched RenderDefinition. `adapter` is one
                      of `inline` / `configMapRef` / `gitRef` / `s3Ref`; `revision` is the
                      adapter-stable identity (sha, etag, resourceVersion, content hash).
                    properties:
                      adapter:
                        type: string
                      revision:
                        type: string
                    required:
                    - adapter
                    - revision
                    type: object
                required:
                - observed
                type: object
              observedGeneration:
                format: int64
                nullable: true
                type: integer
              phase:
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: MarsService
        type: object
    served: true
    storage: true
    subresources:
      status: {}
