[Unit]
Description=MARS local PostGIS
Requires=mars-config.service
After=mars-config.service

[Kube]
Yaml=%h/.local/share/mars-dev/manifests/postgis.yaml
Network=mars.network

[Service]
Restart=on-failure

[Install]
WantedBy=default.target
