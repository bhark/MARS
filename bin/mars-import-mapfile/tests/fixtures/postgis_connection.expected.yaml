# Generated by mars-import-mapfile
# Operator metadata below uses ${VAR:-default} placeholders.
# Review and replace before production use.

service:
  name: "pg"
  title: "Postgis Connection"
  abstract: "Imported from mapfile"
  contact_email: ops@example.org

sources:
  - id: default
    type: postgis
    dsn: "host=db.example.org port=5432 dbname=gis user=mars password=secret"
    native_crs: ${MARS_NATIVE_CRS:-EPSG:25832}

artifacts:
  store:
    type: fs
    path: "${MARS_STORE_PATH}"
  cache:
    path: "${MARS_CACHE_PATH}"
    max_size: 256MiB
    eviction: lru

scales:
  bands:
    - { name: detail, max_denom_exclusive: 2500 }
    - { name: hi, max_denom_exclusive: 12500 }
    - { name: mid, max_denom_exclusive: 50000 }
    - { name: lo, max_denom_exclusive: 250000 }
    - { name: overview, max_denom_exclusive: 10000000 }

cells:
  grid: regular
  origin: [0, 0]
  size_per_band:
    detail: ${MARS_CELL_SIZE:-1024m}
    hi: ${MARS_CELL_SIZE:-1024m}
    mid: ${MARS_CELL_SIZE:-1024m}
    lo: ${MARS_CELL_SIZE:-1024m}
    overview: ${MARS_CELL_SIZE:-1024m}
  extent:
    min_x: ${MARS_EXTENT_MIN_X:-0}
    min_y: ${MARS_EXTENT_MIN_Y:-0}
    max_x: ${MARS_EXTENT_MAX_X:-0}
    max_y: ${MARS_EXTENT_MAX_Y:-0}

interfaces:
  wms:
    enabled: true
    versions: ["1.3.0"]
    formats: ["image/png", "image/jpeg"]

styles:
  line_roads_class_l16:
    type: line
    fill: "#000000"
    stroke_width: 1
  poly_buildings_class_l31:
    type: polygon
    fill: "#808080"

layers:
  - name: "roads"
    type: line
    sources:
      - { band: detail, from: "roads", geometry_column: "geom" }
      - { band: hi, from: "roads", geometry_column: "geom" }
      - { band: mid, from: "roads", geometry_column: "geom" }
      - { band: lo, from: "roads", geometry_column: "geom" }
      - { band: overview, from: "roads", geometry_column: "geom" }
    classes:
      - { name: "class_l16", style: { type: ref, name: "line_roads_class_l16" } }
  - name: "buildings"
    type: polygon
    sources:
      - { band: detail, from: "buildings", geometry_column: "geom" }
      - { band: hi, from: "buildings", geometry_column: "geom" }
      - { band: mid, from: "buildings", geometry_column: "geom" }
      - { band: lo, from: "buildings", geometry_column: "geom" }
      - { band: overview, from: "buildings", geometry_column: "geom" }
    classes:
      - { name: "class_l31", style: { type: ref, name: "poly_buildings_class_l31" } }

observability:
  log_level: info
  log_format: text
